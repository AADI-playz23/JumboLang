name: JumboLang V1 Cross-Platform Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    # --- FIX 1: GIVE THE ROBOT PERMISSION TO CREATE RELEASES ---
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # --- LINUX BUILD ---
      - name: Build JumboLang (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update && sudo apt-get install -y build-essential
          g++ src/*.cpp src/features/*.cpp -o jumbol -Iinclude -O3

      # --- WINDOWS BUILD (Static Linking Fix) ---
      - name: Set up MSYS2 (Windows)
        if: matrix.os == 'windows-latest'
        uses: msys2/setup-msys2@v2
        with:
          install: mingw-w64-x86_64-gcc

      - name: Build JumboLang (Windows)
        if: matrix.os == 'windows-latest'
        shell: msys2 {0}
        run: |
          g++ src/*.cpp src/features/*.cpp -o jumbol.exe -Iinclude -O3 \
              -static -static-libgcc -static-libstdc++ -lws2_32

      # --- FIX 2: UPGRADE TO V4 ARTIFACTS ---
      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: binary-${{ matrix.os }}
          path: |
            jumbol
            jumbol.exe

      # --- RELEASE STEP ---
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            jumbol
            jumbol.exe
          fail_on_unmatched_files: false
          name: JumboLang V1-Prototype-Portable
          body: |
            🐘 JumboLang V1.0.6 is officially Portable!
            - Fixed: 'libstdc++-6.dll' and 'libgcc_s_seh-1.dll' not found errors.
            - Fixed: GitHub 403 Permission error.
            - Optimized: O(1) Hash Map tag lookups.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
